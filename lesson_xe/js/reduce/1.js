function reduce(arr, reduceCallback, initialValue){
  // 参数校验
  if(!Array.isArray(arr) || !arr.length || typeof reduceCallback !== 'function') return []
  else {
    // initialValue 0 1
    // 1. initalValue false 少执行一次
    // 45
    // 2. reduceCallback 怎么合并
    // 3. 返回 值
    let value;
    
    return value;
  }
}

[1,2,3].reduce()
